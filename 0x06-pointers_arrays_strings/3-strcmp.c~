/*header*/
#include <stdio.h>
#include <string.h>

int _strcmp(char *s1, char *s2 /**
			       *main entry
			       */)
{
while (*s1 && (*s1 == *s2))
{
s1++;
s2++;
}
return (*(const unsigned char *)s1 -*(const unsigned char *)s2);
}
